const rulesCharacters = {
  'using a class': [
    `Each class increases a skills and may also learn spells as well as get equipment.`
  ],
  'custom classes': [
    `There are several rules for custom classes different from the original rules. You can enable custom rules when building a character by checking them at the top of the character sheet.`
  ]
}

export default rulesCharacters
