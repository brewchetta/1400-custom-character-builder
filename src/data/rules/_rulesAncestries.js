const rulesAncestries = {
  ancestry: [
    `A character's ancestry can grant them special abilities.`
  ],
  'custom ancestries': [
    `There are several rules for custom ancestries different from the original rules. You can enable custom rules when building a character by checking them at the top of the character sheet.`
  ]
}

export default rulesAncestries
